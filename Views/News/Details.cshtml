@model Fochso.Models.News.NewsViewModel
@{
    ViewData["Title"] = "News Details";
}

<div class="body">


    @if (Model is not null)
    {
        <dl class="row">
            <dt class="col-sm-4">
                News Title:
            </dt>

            <dd class="col-sm-8">
                @Model.Title
            </dd>

            <dt class="col-sm-4">
                News Content:
            </dt>

            <dd class="col-sm-8">
                @Model.Description
            </dd>
        </dl>
        <br />
        <div class="btn-group">
            <a asp-controller="News" asp-action="Update" asp-route-id="@Model.Id" class="btn btn-success">Update</a>
            &nbsp;
            <form asp-controller="News" asp-action="Delete" asp-route-id="@Model.Id">
                <button type="submit" class="btn btn-danger">Delete</button>
            </form>
            &nbsp;
            <a asp-controller="News" asp-action="Index" class="btn btn-outline-secondary">Go back to list</a>
        </div>
    }
</div>
